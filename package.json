{
  "name": "broke",
  "version": "0.5.1",
  "description": "Token budget extension for AiderDesk: progressive input compression (structural + truncation + summarization), local-model (Ollama) summarization offload, and token-saving workflows",
  "main": "index.ts",
  "scripts": {
    "test": "tsx --test \"tests/*.test.ts\"",
    "typecheck": "tsc --noEmit",
    "bench": "tsx scripts/bench.ts",
    "measure": "tsx scripts/measure.ts"
  },
  "keywords": [
    "aiderdesk",
    "extension",
    "tokens",
    "compression",
    "ollama",
    "local-model"
  ],
  "author": "777marvin",
  "license": "MIT",
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@aiderdesk/extensions": "^0.30.0",
    "@types/node": "^26.2.0",
    "tsx": "^4.23.11",
    "typescript": "^7.0.2"
  }
}
